import React from "react";

const Dashboard = () => {
  return (
    <div style={{ minHeight: '100vh', display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: 24 }}>
      Guarda Dashboard Placeholder<br />
      (Integrate real dashboard here)
    </div>
  );
};

export default Dashboard; 